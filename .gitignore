# ── RStudio / R temporary files ───────────────────────────────────────────
.Rhistory
.Rapp.history
.RData
.Ruserdata
.Rproj.user/
.Renv

# ── OS cruft ──────────────────────────────────────────────────────────────
.DS_Store

# ── Build artifacts ───────────────────────────────────────────────────────
.Rcheck/
packrat/
renv/library/
renv/staging/

# ── Data & large result objects (never distributed) ───────────────────────
# Real student data must never be committed.
RWD/
data/
*.xlsx
*.xls
*.csv

# Simulation outputs / intermediate objects (regenerated by analysis/01-02)
RData/
*.rds

# Generated figures
*.png

# ── Git stray file accidentally tracked in earlier history ────────────────
FETCH_HEAD
